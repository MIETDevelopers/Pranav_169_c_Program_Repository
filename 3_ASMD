#include <stdio.h>
#include <math.h>
int main()
{
	int num1, num2;
	int sum, sub, mult, division;
	
	printf("Enter two integer number ");
	scanf("%d,%d", &num1, &num2);
	sum=num1 + num2;
	sub=num1 - num2;
	mult=num1 * num2;
	division = num1/num2;
	printf( "the sum of the given numbers : %d\n", sum);
	printf("the difference of the given numbers : %d\n", sub);
	printf("the product of the given numbers : %d\n",mult);
	printf("the quoitent of the given numbers : %d\n", division);
	return 0 ;#include<stdio.h>
#i
	
}